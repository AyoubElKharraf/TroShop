# Laisser vide en local : Vite proxy `/api` et `/uploads` vers le backend (port 3001).
# Si le front est servi ailleurs sans proxy, définir l’URL complète du backend :
# VITE_API_URL=http://localhost:3001
